"""FROGÉ HQ Agent Brain — controlled perception → planning → action → observation → verification loop.

The AI proposes. The backend validates. Tools execute. The system observes. QA verifies.
Maya coordinates. The user remains in control. No private chain-of-thought is exposed —
only decision summaries, plans, actions, evidence, permissions, results and confidence.
"""
